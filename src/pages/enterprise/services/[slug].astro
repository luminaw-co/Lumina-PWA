---
import { services } from "@/data/services";
import BaseLayout from "@/layouts/BaseLayout.astro";

export async function getStaticPaths() {
	return services.map((service) => ({
		params: { slug: service.slug },
		props: { service },
	}));
}

const { service } = Astro.props;
---

<BaseLayout
	title={`${service.title} - Lúmina Enterprise`}
	description={service.description}
>
	<main class="min-h-screen bg-[var(--bg-page)] px-6 py-[var(--space-64)]">
		<h1 class="text-4xl font-bold mb-8">{service.title}</h1>
		<p class="text-lg mb-16">{service.description}</p>
		<img
			src={service.image}
			alt={service.title}
			class="w-full max-w-4xl mx-auto rounded-lg shadow-lg"
		/>
	</main>
</BaseLayout>
