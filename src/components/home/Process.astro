---
import TitleUI from "@/components/ui/TitleUI.astro";
import Chip from "@/components/ui/Chip.astro";
import Link from "@/components/ui/Link.astro";

const steps = [
	{
		title: "El punto de partida",
		desc: "Nos cuentas qué necesitas, qué quieres lograr y en qué punto está tu proyecto. Sin tecnicismos, sin rodeos. Solo claridad desde el primer minuto.",
	},
	{
		title: "La ruta ideal",
		desc: "Analizo tu caso y te presento la opción más eficiente según tu presupuesto, tu tiempo y tu objetivo. Nada inflado, nada innecesario. Solo lo que realmente te sirve.",
	},
	{
		title: "El viaje compartido",
		desc: "Trabajo contigo paso a paso. Te muestro avances, explico decisiones y te mantengo al tanto del progreso. Todo lo ves, todo se valida, nada te toma por sorpresa.",
	},
	{
		title: "La llegada segura",
		desc: "Te entrego tu producto funcionando, organizado y con instrucciones simples para que puedas usarlo sin depender de nadie. Y, si lo necesitas, seguimos trabajando juntos.",
	},
];
---

<section class="flex flex-col gap-5 py-8 w-full max-w-[1424px] m-auto">
	<div class="ctn-hero !justify-end">
		<TitleUI
			title="El viaje del proyecto"
			subtitle="En cuatro sencillos pasos"
		/>
	</div>
	<div class="mt-6 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
		{
			steps.map((step, i) => (
				<article
					class="flex flex-col gap-3 p-6 rounded-2xl border border-[var(--brand-blue)] shadow-[var(--shadow-base)] hover:translate-x-1 hover:translate-y-1 transition-transform hover:bg-[var(--bg-card)]"
					aria-label={`Paso ${i + 1}`}
					data-idx={i}
				>
					<div class="flex items-end gap-3">
						<Chip class="flex gap-4 !font-semibold !text-[var(--accent-link)]">
							{`Paso ${i + 1}`}
						</Chip>
					</div>
					<h3 class="text-lg text-[var(--text-primary)] font-semibold leading-tight">
						{step.title}
					</h3>
					<p class="text-sm leading-relaxed text-[var(--text-primary)]">
						{step.desc}
					</p>
				</article>
			))
		}
	</div>
	<div class="flex justify-center mt-8">
		<Link href="#contact" class="text-[var(--brand-blue)]">
			Quiero empezar
		</Link>
	</div>
</section>
